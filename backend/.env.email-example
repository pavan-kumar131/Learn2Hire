# MongoDB connection (required)
MONGODB_URI=mongodb+srv://shikhamathur2201_db_user:learn2hire123@cluster0.hjbhl2f.mongodb.net/?appName=Cluster0

# JWT secret for signing login tokens 
JWT_SECRET=learn2hire-jwt-secret-change-in-production

# Server port default is 5000)
PORT=5000

# Signup OTP email (SMTP). Fill SMTP_USER + SMTP_PASS (Gmail: use an App Password, not your normal password).
# Outlook: host smtp.office365.com, same port 587, SMTP_SECURE=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
# Use true for port 465 (SSL); false for 587 (STARTTLS)
SMTP_SECURE=false
SMTP_USER=kumarpavan.0103@gmail.com
# Gmail: MUST be a 16-character "App password" from Google Account → Security → App passwords (NOT your normal login password).
SMTP_PASS=lqmq cidn mopq ibov
# Optional: full From header. If empty, uses SMTP_FROM_NAME + SMTP_USER
SMTP_FROM=kumarpavan.0103@gmail.com
SMTP_FROM_NAME=Learn2Hire

# Set true only to include OTP in API JSON (debug). Use false with real email.
OTP_ECHO_TO_CLIENT=false